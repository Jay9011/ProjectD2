#pragma once

enum class OBJECT_TYPE
{
	BACKGROUND,
	DEFAULT,
	PLATFORM,
	PLAYER,
	FOREBLOCK,
	FOREGROUND,
	FIN
};

enum class PLAYER_STATE
{
	APPEAR,
	IDLE,
	RUN,
	JUMP
};

enum class SIDE
{
    NONE,
	UPPER_SIDE,
    LOWER_SIDE,
    LEFT_SIDE,
    RIGHT_SIDE
};